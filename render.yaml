services:
  web:
    type: web
    name: <ton_nom_de_service>
    env: php
    plan: free
    runtime: php-8.4 # ou la version que tu utilises
    buildCommand: composer install
    startCommand: php -S 0.0.0.0:10000 -t public
